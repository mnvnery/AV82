title: AV82 STUDIO
email: geral@av82studio.com
description: >- # this means to ignore newlines until "baseurl:"
  AV82 é um estúdio de fotografia e vídeo localizado no centro do Porto. Este é um espaço que não só permite a realização de trabalhos fotográficos como também o aluguer de material, seja ele de fotografia ou video.
baseurl: "/AV82"  # the subpath of your site, e.g. /blog
url: "https://mnvnery.github.io" # the base hostname & protocol for your site, e.g. http://example.com


# Build settings
permalink: pretty 
plugins:
  - jekyll-feed

sass:
  sass_dir: _sass

collections:
  photo: 
    output: true
  film:
    output: true
  products:
    output: true
    
collections_config:
  products:
    add_options:
      - name: Add Product
        href: "cloudcannon:explore/_products/.html🆕"

collections_config:
  products:
    add_options:
      - name: Add New Product
        href: "cloudcannon:explore/_products/.html🆕"
    label: Product
    hide_body: true
    fields:
    - name: layout
      type: text
      config:
        required: false
      label: layout
      default: project
      hidden: true
    - name: title
      type: text
      config:
        required: false
      label: Name
    - name: description
      type: textarea
      default: ''
      config:
        required: false
        wysiwyg: true
        schema:
          format: html-blocks
      label: Description
    - name: type
      type: select
      default: []
      config:
        required: false
        options:
        - Photography
        - Film
        - Studio
        - Crew
        source:
          type: simple
          section: 
          file: 
          path: 
      label: Type
    - name: images
      type: image_gallery
      config: {}
      label: Images
    - name: price_day
      type: number
      label: Price/day
      description: ''
      default: 0
      required: true
      config:
        min: 
        max: 
        step: 
    - name: category
      type: tag_list
      default: []
      label: Category
    - name: recommended
      type: list
      config:
        use_select: true
        min: 
        max: 
        source:
          type: pages
          section: products
      label: Recommended


# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
